import React from 'react';

const Section = ({ titulo }) => {
    return (
        <section>
            <form>
                <h2>{titulo}</h2>
            </form>
        </section>
    )
};

export default Section;